spring:
  application:
    name: site
  datasource:
    url: jdbc:oracle:thin:@(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1521)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=gb91286f3a5c4e2_oracledb_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: admin
    password: '!@private1AB'
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: 네이버_클라이언트_ID
            client-secret: 네이버_클라이언트_비밀키
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope: name,email
            client-name: Naver
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  mail:
    host: smtp.gmail.com
    port: 587
    username: hong837198@gmail.com
    password: ktjcikdttdsatxnt
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout : 5000
          writetimeout : 5000
    auth-code-expiration-millis : 1800000
  data:
    redis:
      host: localhost
      port: 6379